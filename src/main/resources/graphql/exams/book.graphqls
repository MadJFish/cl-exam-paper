type Book {
    id: ID
    exams: [Exam]
    authorId: ID
    name: String
    description: String
    price: Float
    isActive: Boolean
}

input BookInput {
    consistsOf: [ID!]
    name: String
    description: String
    price: Float
}